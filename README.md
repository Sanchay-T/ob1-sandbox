# ob1-sandbox

Sandbox repo for testing ob1 AI-generated pull requests.

- Frontend scaffold: Vite + React in `frontend/`
- Default branch: `main`
- QA workflow: GitHub Actions builds + runs Playwright (`npm run qa:test`) and uploads a login demo video on every PR.
